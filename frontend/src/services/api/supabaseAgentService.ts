/**
 * Backward-compatible service name retained for older extensions.
 * The platform now uses the REST backend for persisted agent data.
 */
export { RestAgentService as SupabaseAgentService } from './restAgentService';
